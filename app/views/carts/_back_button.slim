.ui.left.rail.attached#back-button
  = link_to location_drugs_path(@location = @cart.line_items.nil? ?  @cart.line_items.last.price.location_id : 1), method: "get", class: "ui icon big button blue right floated" do
    i.icon.arrow.left
